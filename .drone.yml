---
kind: pipeline
type: kubernetes
name: build-fpm-centos-8

trigger:
  branch:
    - master
  event:
    include:
      - push

steps:
  - name: fpm-centos 8
    image: docker
    environment:
      REPO: quay.io
    settings:
      username:
        from_secret: QUAYIO_DOCKER_USERNAME
      password:
        from_secret: QUAYIO_DOCKER_PASSWORD
    volumes:
      - name: dockersock
        path: /var/run
    commands:
    # wait for docker to start
    - sleep 2
    - docker pull $REPO/gravitational/fpm-centos:8 || true
    - cd centos/8 && docker build -t $REPO/gravitational/fpm-centos:8 .
    - docker login -u="$PLUGIN_USERNAME" -p="$PLUGIN_PASSWORD" $REPO
    - docker push $REPO/gravitational/fpm-centos:8

services:
  - name: Start Docker
    image: docker:dind
    volumes:
      - name: dockersock
        path: /var/run

volumes:
  - name: dockersock
    temp: {}

---
kind: pipeline
type: kubernetes
name: build-fpm-debian-8

trigger:
  branch:
    - master
  event:
    include:
      - push

steps:
  - name: fpm-debian 8
    image: docker
    environment:
      REPO: quay.io
    settings:
      username:
        from_secret: QUAYIO_DOCKER_USERNAME
      password:
        from_secret: QUAYIO_DOCKER_PASSWORD
    volumes:
      - name: dockersock
        path: /var/run
    commands:
    # wait for docker to start
    - sleep 2
    - docker pull $REPO/gravitational/fpm-debian:8 || true
    - cd debian/8 && docker build -t $REPO/gravitational/fpm-debian:8 .
    - docker login -u="$PLUGIN_USERNAME" -p="$PLUGIN_PASSWORD" $REPO
    - docker push $REPO/gravitational/fpm-debian:8

services:
  - name: Start Docker
    image: docker:dind
    volumes:
      - name: dockersock
        path: /var/run

volumes:
  - name: dockersock
    temp: {}

---
kind: signature
hmac: b27cd1722b7a3dc282e73ae61d807abb78f745603ffed522ea11d31ebc713bce

...
